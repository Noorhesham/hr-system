/**
 * Re-export — active implementation lives in `../PhoneInput`.
 * Prefer: `import { PhoneInput } from "@/components/form"`
 */
export { PhoneInput as default, PhoneInput } from "../PhoneInput"
export type { PhoneInputProps } from "../PhoneInput"
