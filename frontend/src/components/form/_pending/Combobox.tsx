"use client"

/**
 * Re-export: use `@/components/form` Combobox (TanStack Query lazy + Base UI).
 * Kept so older `_pending` imports keep working.
 */
export { Combobox } from "../Combobox"
export type {
  ComboboxProps,
  ComboboxOption,
  ComboboxPage,
} from "../Combobox"
